/* My Memo — 런타임 설정 (자리표시자).
 *
 * 이 파일은 저장소에 커밋되는 "빈" 기본값입니다. token 은 항상 비워 둡니다.
 *
 * GitHub Pages 배포 시(.github/workflows/deploy.yml)에는 owner/repo/branch 만
 * 자동으로 채워 배포되고, token 은 비워 둡니다.
 *
 * ⚠ 토큰을 여기(또는 배포물)에 넣지 마세요. 배포된 config.js 는 공개적으로
 *   내려받을 수 있어, 실린 토큰은 GitHub 보안 스캐닝에 의해 자동 폐기됩니다.
 *   쓰기(저장/수정/삭제)용 토큰은 브라우저 ⚙ 관리자 설정에 입력하면 해당
 *   브라우저 localStorage 에만 저장되어 노출되지 않습니다.
 */
window.MYMEMO_CONFIG = {
  owner: "",
  repo: "",
  branch: "",
  token: ""
};
